<% @flats.each do |flat| %>
  <%= flat.title %>
  <%= flat.address %>
  <%= flat.description %>
  <%= link_to 'Show', flat %></td>
  <%= link_to 'Edit', edit_flat_path(flat) %></td>
  <%= link_to 'Destroy', flat, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>